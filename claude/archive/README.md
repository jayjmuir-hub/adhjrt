# claude/archive/ — removed

This folder held material that was already superseded: old status reports,
earlier versions of documents, and notes kept "just in case". Anything in it
that was still true was carried into `RESTORE.md`, a card in
`claude/decisions/` or a runbook in `claude/runbooks/` by the documentation cut
(`claude/decisions/2026-09-10-documentation-cut.md`); the rest was history. It
is in git history before the cut commit. **Do not recreate this folder**: git
history is the archive.
